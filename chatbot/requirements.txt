# Minimal runtime deps for a standalone chatbot image (inference + retrieval + UI).
# Aligns with frontend/ + agent_server + config_LangGraph when those sources are packaged.
# No Jupyter, PDF loaders, unstructured, or arxiv tooling.

gunicorn==23.0.0
uvicorn==0.32.0
sse-starlette==2.1.3

fastapi==0.115.3
pydantic==2.10.6
gradio==5.36.2

langserve==0.3.1
langchain==0.3.4
langchain-text-splitters==0.3.8
langchain_community==0.3.3
openai==1.52.1

langchain-nvidia-ai-endpoints==0.3.9

faiss-cpu==1.9.0

langgraph>=0.2.0,<0.7
langchain-core>=0.3.0,<0.4.0
